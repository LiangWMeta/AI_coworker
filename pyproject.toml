[project]
name = "llm-senior"
version = "0.1.0"
description = "Autonomous AI coworker that grows from junior to senior through real work and calibration"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
